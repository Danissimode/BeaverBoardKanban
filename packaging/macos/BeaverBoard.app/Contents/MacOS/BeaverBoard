#!/bin/bash
# Beaver Board macOS Launcher
# Auto-generated wrapper script

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
APP_CONTENTS="$SCRIPT_DIR/.."
RESOURCES_DIR="$APP_CONTENTS/Resources"

# Set up data directories
export HOME_LIBRARY_PATH="$HOME/Library/Application Support/BeaverBoard"
mkdir -p "$HOME_LIBRARY_PATH"

# Set locale for proper UTF-8 handling
export LC_ALL=en_US.UTF-8
export LANG=en_US.UTF-8

# Run the application
exec "$SCRIPT_DIR/publish/KittyClaw.Web" --urls "http://localhost:5230" "$@"